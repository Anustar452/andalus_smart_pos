name: andalus_smart_pos
description: Offline-first POS for Ethiopian small shops
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # 🌿 State Management
  flutter_riverpod: ^2.5.1
  hooks_riverpod: ^2.5.1
  flutter_hooks: ^0.20.5

  # 💾 Local Database
  sqflite: ^2.3.3+1
  path: ^1.9.0

  # 🌐 Networking & API
  dio: ^5.7.0
  retrofit: ^4.1.0
  logger: ^2.0.2

  # 🔐 Security
  flutter_secure_storage: ^9.2.2

  # 🖨️ Bluetooth Printing - REPLACE WITH WORKING PACKAGE
  # bluetooth_print: ^4.3.0  # ❌ REMOVE THIS
  esc_pos_utils: ^1.1.0 # ✅ ADD THIS - For receipt formatting
  network_to_file_image: ^3.0.2 # ✅ ADD THIS - Fallback

  # 🌍 Localization
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # 🔄 JSON Serialization
  json_annotation: ^4.9.0
  freezed_annotation: ^2.4.1

  # 🧭 Navigation
  go_router: ^14.2.0
  shared_preferences: ^2.2.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.11
  retrofit_generator: ^7.0.8
  json_serializable: ^6.9.0
  freezed: ^2.4.2

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/
